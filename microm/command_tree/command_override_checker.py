# decorator that checks if the function is overriden by the user in the path specified by some static config + the env var for microm.. if it exists, run that one. if it doesnt run the builtin one.


def command_override_checker():
    pass
